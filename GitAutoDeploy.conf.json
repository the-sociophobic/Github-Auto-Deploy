{
	"port": 8001, 
	"repositories": 
	[{
		"url": "https://github.com/my-repo", 
		"path": "/var/www/my-repo",
		"deploy": "sudo service my-repo stop && sudo kill $(sudo lsof -t -i:3000) && cd /var/www/my-repo && git pull origin master && yarn && sudo service my-repo start"
	}]
}
